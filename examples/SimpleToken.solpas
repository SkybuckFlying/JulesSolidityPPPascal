contract SimpleToken;

var
  owner: address;
  totalSupply: cardinal;

procedure create();
begin
  // This contract is simplified to demonstrate the compiler's
  // core functionality with simple variable assignments.
  // Full features like complex assignments and events are not yet supported.
  totalSupply := 1000;
end;

end.