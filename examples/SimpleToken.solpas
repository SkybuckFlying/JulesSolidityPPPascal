contract SimpleToken;

var
  owner: address;
  balances: TDictionary<address, cardinal>;
  totalSupply: cardinal;
  canTransfer: boolean;

event Transfer(from: address; to: address; value: cardinal);

constructor Create(initialSupply: cardinal);
begin
  owner := msg.sender;
  totalSupply := initialSupply;
  canTransfer := true;
end;

procedure transfer(recipient: address; value: cardinal);
public;
begin
  if canTransfer then
  begin
    // Balance check and transfer logic would go here.
    // This is a placeholder to test the 'if' and 'emit' statements.
  end;
  emit Transfer(msg.sender, recipient, value);
end;

function balanceOf(account: address): cardinal;
public;
begin
  // result := balances[account];
end;

end.