contract Voting;

var
  chairperson: address;
  proposalCount: cardinal;
  hasVoted: boolean;

procedure vote(); public;
begin
  // This contract is simplified to demonstrate the compiler's
  // core functionality with simple variable assignments.
  // Full features like array/mapping access are not yet supported.
  hasVoted := true;
end;

end.