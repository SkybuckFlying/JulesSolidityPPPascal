contract Voting;

var
  chairperson: address;
  voterAllowed: TDictionary<address, boolean>;
  hasVoted: TDictionary<address, boolean>;

constructor Create();
begin
  chairperson := msg.sender;
end;

procedure giveRightToVote(voter: address);
begin
  // A real implementation would check if sender is chairperson.
  // if msg.sender <> chairperson then ...

  if voterAllowed[voter] then
  begin
    // In a real contract, this would raise an exception.
  end;
  voterAllowed[voter] := true;
end;

procedure vote(proposal: cardinal);
begin
  if hasVoted[msg.sender] then
  begin
     // In a real contract, this would raise an exception.
  end;
  hasVoted[msg.sender] := true;
end;

end.